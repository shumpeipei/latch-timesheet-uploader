[project]
name = "latch-timesheet-uploader"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "google-auth",
    "google-auth-httplib2",
    "google-api-python-client",
    "playwright",
]
